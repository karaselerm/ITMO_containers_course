FROM python

LABEL description="BAD DOCKERFILE"

WORKDIR /app
USER root


RUN apt update && apt -y install git vim nano 

COPY app/ .


COPY requirements.txt .
RUN pip install -r requirements.txt



CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]